modules = ["python-3.11"]

run = "python main.py"

[nix]
channel = "stable-24_05"

[deployment]
deploymentTarget = "autoscale"
run = ["sh", "-c", "gunicorn --bind 0.0.0.0:$PORT --workers 2 api.index:app"]

[[ports]]
localPort = 8080
externalPort = 80

[env]
PORT = "8080"
